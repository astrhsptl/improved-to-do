import { FC } from 'react';
import { NavbarStyles } from '@/shared';

export const NavbarLogo: FC = () => {
  return (
    <section className={NavbarStyles.navLogoContainer}>
      {/*<img src='/task-logo.svg' alt='' />*/}
      {/*SmartTask*/}
    </section>
  );
};
